"prachtig voorbeeld van een artiesten presentatiemodel"
presentation RecordshopArtistPresentation;

from RecordShopDataContract import ArtistDto    ;

/* Disabled. Enable when fixed.

"Simple Artist Form"
ContentForm ArtistForm context ArtistDto [
	element age
		label artistAge;
]

CompoundDialogue ArtistCompound context ArtistDto 
	readonly true;
	dialogues [
		ArtistForm;
	]
	
*/