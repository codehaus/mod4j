/** Generated by Mod4j at Tue May 20 16:10:58 CEST 2008
 */
package org.company.recordshop.data.spring.dao;

import java.util.Iterator;
import java.util.List;

import org.company.recordshop.domain.Person;

import org.springframework.orm.hibernate3.support.HibernateDaoSupport;

/**
 * Person Data Access Object (DOA) Implementation. Responsible for Creating,
 * Reading, Updating and Deleting Person objects in the database. All database
 * access is done here.
 *  
 * @author Generated by Mod4j
 */
public class PersonDaoImpl extends HibernateDaoSupport implements PersonDao {

	public Person retrieve(long id) {
		Object o = getHibernateTemplate().get(Person.class, new Long(id));
		if (o == null) {
			return null;
		}
		return (Person) o;
	}

	public Person add(Person object) {
		getHibernateTemplate().save(object);
		return object;
	}

	public Person update(Person object) {
		getHibernateTemplate().save(object);
		return object;
	}

	public void delete(Person person) {
		getHibernateTemplate().delete(person);
	}

	@SuppressWarnings("unchecked")
	public List<Person> listAllPersons() {
		return getHibernateTemplate().find("from Person");
	}
}
